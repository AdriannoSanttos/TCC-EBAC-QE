{
  "name": "tcc-ebac-api-cupons",
  "version": "1.0.0",
  "description": "Testes de API - US-0003 Cupons",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:verbose": "jest --verbose",
    "test:report": "jest --json --outputFile=reports/test-results.json"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ],
    "verbose": true,
    "collectCoverage": false
  }
}